# klemer-agents.md

Use 📄 `anti-plenger.md` for all layers.

**L1: The Contract**
But Do NOT write implementation logic. Write ONLY compilable interfaces, types, skeletons, and empty method signatures as "cheatsheet".

**L2: The Skeleton & Flow (Wiring)**
Define the internal state. Wire the entire execution path. Leave all complex algorithms as `// TODO`.

**L3: The Meat (Deep Implementation)**
Fill in the final TODOs. Leave zero unwritten logic, stubs, band-aids, or 📄 `plenger-traits.md`.
